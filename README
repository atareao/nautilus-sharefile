Nautilus-Sharefile
=================

Nautilus-Sharefile

Download
--------

Download the [package][dlp] *or* checkout the source:

    $ git clone https://github.com/atareao/nautilus-sharefile.git

Installation
------------

* Nautilus

```
sudo add-apt-repositoy ppa:atareao/nautilus-extensions
sudo apt-get update
sudo apt-get install nautilus-sharefile

* Nemo

```
sudo add-apt-repositoy ppa:atareao/nemo-extensions
sudo apt-get update
sudo apt-get install nemo-sharefile

* Caja

```
sudo add-apt-repositoy ppa:atareao/caja-extensions
sudo apt-get update
sudo apt-get install caja-sharefile
```
